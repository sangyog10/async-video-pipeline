apiVersion: 1

providers:
  - name: 'video-pipeline'
    orgId: 1
    folder: 'Video Pipeline'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
